{
  "name": "@data-proxy/root",
  "private": true,
  "version": "0.0.0",
  "author": "Loïc Mahieu <mahieuloic@gmail.com>",
  "license": "MIT",
  "scripts": {
    "test": "yarn lint && lerna run --stream --concurrency 1 test",
    "lint": "eslint .",
    "build": "lerna run --stream build",
    "start": "env-cmd -- lerna run --stream --no-sort start"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^8.57.0",
    "@typescript-eslint/parser": "^8.57.0",
    "env-cmd": "^11.0.0",
    "eslint": "^9.38.0",
    "lerna": "^9.0.5",
    "typescript": "^5.9.3"
  }
}
