{
  "name": "@data-proxy/react-admin-provider",
  "version": "0.3.0",
  "files": [
    "lib/*"
  ],
  "main": "./lib/index.js",
  "typings": "./lib/index.d.ts",
  "author": "Loïc Mahieu <mahieuloic@gmail.com>",
  "license": "MIT",
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "test": "npm run build",
    "build": "rm -rf lib/* && tsc",
    "watch": "yarn build && tsc -w",
    "prepare": "yarn build"
  },
  "dependencies": {
    "@react-admin-git-provider/common": "^0.2.0",
    "@react-admin-git-provider/gitlab": "^0.3.0"
  },
  "devDependencies": {
    "tslint": "^5.20.0",
    "typescript": "^3.6.3"
  }
}
